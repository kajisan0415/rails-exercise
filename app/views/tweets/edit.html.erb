<body class="tweet-body">
  <header class="index-header">
    <nav class="header-item">
      <div class="nav-list">
        <ul class="nav-list-item">
          <%= link_to 'Home', root_path,class:"fas" %>
        </ul>
        <ul class="nav-list-item">
          <%= link_to '新規投稿', new_tweet_path,class:"fas" %>
        </ul>
        <ul class="nav-list-item">
          <%= link_to '投稿一覧', tweets_path,class:"fas" %>
        </ul>
      </div>
    </nav>
  </header>
  <main class="form-body">
    <div class="form-container">
      <h1 class="form-title">編集</h1>
      <%= form_with(model: @tweet, local: true) do |form| %>
        <% if @tweet.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@tweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>
            <ul>
            <% @tweet.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <div class="field">
          <%= form.label :title,"名前" %>
          <%= form.text_field :title,class:"form-tweet-title" %>
        </div>

        <div class="field">
          <%= form.label :content,"内容" %>
          <%= form.text_area :content,class:"form-tweet-body" %>
        </div>

        <%= attachment_image_tag @tweet, :image,:fill, 350, 300, fallback: "no_image.jpg"  %>

        <div class="actions">
          <%= form.submit '更新',class:"submit-button" %>
        </div>
      <% end %>

      <div class="bottom-action">
        <%= link_to '戻る', @tweet,class:"show-item"  %> |
        <%= link_to '一覧', tweets_path,class:"show-item"  %>
      </div>

    </div>
  </main>
  <footer></footer>
</body>

